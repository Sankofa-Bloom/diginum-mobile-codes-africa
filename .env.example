# DigiNum Frontend Environment Variables (.env.example)
# These values are exposed to the browser and should be safe to expose

# API Configuration
VITE_API_BASE_URL=http://localhost:4000/api

# Supabase (Public keys only)
VITE_SUPABASE_URL=https://your-supabase-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# Stripe (Public key only)
VITE_STRIPE_PUBLIC_KEY=your-stripe-publishable-key

# Note: All sensitive credentials are stored in backend/.env only
# Do not add any secret keys, service role keys, or API secrets here
